apiVersion: apps/v1
kind: Deployment
metadata:
  name: powerfulfamily-deployment
  labels:
    app: powerfulfamily-blog
spec:
  replicas: 2
  selector:
    matchLabels:
      app: powerfulfamily_blog
  template:
    metadata:
      labels:
        app: powerfulfamily_blog
    spec:
      containers:
      - name: powerfulfamily-blog
        image: ghcr.io/masebb/powerfulfamily.net:latest
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  labels:
    name: powerfulfamily-blog
  name: powerfulfamily-blog
spec:
  selector:
    app: powerfulfamily_blog
  ports:
    - targetPort: http
      port: 80
